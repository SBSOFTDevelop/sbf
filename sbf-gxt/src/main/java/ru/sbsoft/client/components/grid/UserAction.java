package ru.sbsoft.client.components.grid;

/**
 * Флаги навигации по набору записей
 * @author balandin
 */
public enum UserAction {

    NONE,
    GOTO_FIRST_RECORD,
    GOTO_LAST_RECORD,
    GOTO_PREV_RECORD,
    GOTO_NEXT_RECORD,
    RESELECT;
}
